trigger:
  branches:
    include:
      - main

parameters:
  - name: Test
    type: boolean
    default: true

jobs:
  - job: Build
    steps:
       - script: echo $(Foo)
    condition: and(succeeded(), eq('${{ parameters.Test }}', 'true'))
  - script: echo "Foo is false"
    condition: and(succeeded(), eq('${{ parameters.Test }}', 'false'))
